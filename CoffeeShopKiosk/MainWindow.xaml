<Window x:Class="CoffeeShopKiosk.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:CoffeeShopKiosk.Views"
    Title="Coffee Shop Kiosk" WindowState="Maximized" WindowStyle="SingleBorderWindow" ResizeMode="CanResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Coffee-themed header -->
        <Border Grid.Row="0" Background="#4B2E19" CornerRadius="0,0,32,32" Height="100" VerticalAlignment="Top" Effect="{DynamicResource DropShadowEffect}">
            <DockPanel LastChildFill="True">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="30,0,0,0">
                    <!-- Coffee cup icon (SVG Path) -->
                    <Viewbox Height="48" Width="48" Margin="0,0,16,0">
                        <Canvas Width="48" Height="48">
                            <Path Data="M12,40 Q24,48 36,40 Q36,24 24,24 Q12,24 12,40 Z M24,24 Q24,12 36,12 Q36,24 24,24 Z" Fill="#FFF8F0" Stroke="#C7A17A" StrokeThickness="2"/>
                            <Ellipse Canvas.Left="18" Canvas.Top="8" Width="12" Height="8" Fill="#C7A17A" Opacity="0.7"/>
                        </Canvas>
                    </Viewbox>
                    <TextBlock Text="Welcome to the Coffee Shop!" FontSize="28" FontWeight="Bold" Foreground="#FFF8F0" VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </Border>
        <!-- Minimal image test -->
        <Image Source="pack://siteoforigin:,,,/CoffeeShopKiosk/Images/Espresso.jpg" Width="100" Height="100" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20"/>
        <local:ProductListView Grid.Row="1" />
        <Grid.Resources>
            <DropShadowEffect x:Key="DropShadowEffect" Color="#000" BlurRadius="16" ShadowDepth="2" Opacity="0.18" />
        </Grid.Resources>
    </Grid>
</Window>